AWSTemplateFormatVersion: '2010-09-09'
Description: >
  Tailnet monitoring node. Graviton EC2 instance joined to your Tailscale tailnet
  with ZERO inbound security-group rules. Runs Netdata (streaming parent for home
  nodes), Uptime Kuma (synthetic checks against home hosts), ntfy (alert fan-out)
  and optionally Loki + Grafana for log aggregation. All service ports are bound
  to the Tailscale interface address only.

Metadata:
  AWS::CloudFormation::Interface:
    ParameterGroups:
      - Label: {default: Network}
        Parameters: [NetworkStackName, AssignPublicIpv4]
      - Label: {default: Instance}
        Parameters: [InstanceType, DeploymentMode, DataVolumeSizeGb, DataVolumeDeleteOnTermination, LatestUbuntuAmi]
      - Label: {default: Tailscale}
        Parameters: [TailscaleHostname, TailscaleTag, TailscaleAuthKeyParam]
      - Label: {default: Admin access}
        Parameters: [AdminUsername, AdminSshPublicKey]
      - Label: {default: Secrets and alerting}
        Parameters: [NetdataStreamKeyParam, GrafanaPasswordParam, AlertEmail]
      - Label: {default: Diagnostics}
        Parameters: [BootstrapLogGroupName]

Parameters:
  NetworkStackName:
    Type: String
    Default: mon-network
    Description: Name of the 01-network.yaml stack to import VPC/subnet from.

  InstanceType:
    Type: String
    Default: t4g.medium
    Description: >
      Graviton only (the AMI is arm64). t4g.nano is fine for WatchdogOnly.
      FullStack (Loki + Grafana + Netdata parent) wants t4g.medium minimum.
    AllowedValues: [t4g.nano, t4g.micro, t4g.small, t4g.medium, t4g.large, t4g.xlarge]

  DeploymentMode:
    Type: String
    Default: FullStack
    Description: >
      WatchdogOnly = Netdata + Uptime Kuma + ntfy (cheap external eyes).
      FullStack    = adds Loki + Grafana for centralised logs and dashboards.
    AllowedValues: [FullStack, WatchdogOnly]

  DataVolumeSizeGb:
    Type: Number
    Default: 100
    MinValue: 8
    MaxValue: 1000
    Description: gp3 data volume mounted at /data (Netdata dbengine, Loki, Kuma, Grafana). ~$0.08/GB-mo.

  DataVolumeDeleteOnTermination:
    Type: String
    Default: 'false'
    Description: >
      false keeps your metric/log history if the instance is replaced or the stack
      is deleted -- but leaves an orphaned volume you keep paying for. Clean it up
      by hand when you are done with it.
    AllowedValues: ['true', 'false']

  AssignPublicIpv4:
    Type: String
    Default: 'true'
    Description: >
      AWS charges ~$0.005/hr (~$3.65/mo) for a public IPv4 address. Set false to run
      IPv6-only and avoid it -- Tailscale and Ubuntu mirrors both work over IPv6, but
      verify before you rely on it.
    AllowedValues: ['true', 'false']

  TailscaleHostname:
    Type: String
    Default: mon-aws
    Description: MagicDNS hostname this node will claim.
    AllowedPattern: '^[a-z0-9][a-z0-9-]{0,62}$'

  TailscaleTag:
    Type: String
    Default: 'tag:monitor'
    Description: >
      ACL tag to advertise. The auth key MUST be created with this tag, or leave this
      blank to use an untagged (expiring) key. Tagged keys do not expire -- strongly preferred.

  TailscaleAuthKeyParam:
    Type: String
    Default: /monitoring/tailscale-authkey
    Description: >
      SSM Parameter Store SecureString holding the Tailscale auth key. Create it
      BEFORE deploying (see README). Never passed as a CFN parameter value, so it
      does not land in UserData or stack history.

  AdminUsername:
    Type: String
    Default: ''
    Description: >
      Optional. Creates a named local admin you can 'ssh <user>@hostname' as. The
      account has NO PASSWORD -- authentication is Tailscale SSH (tailnet identity)
      and/or the public key below. Leave blank to rely on the AMI's 'ubuntu' user,
      which already has passwordless sudo and is equally usable.
      You must ALSO add this name to the "users" list in the Tailscale ACL ssh block,
      or Tailscale SSH will refuse the target user. See README 2.1.
    AllowedPattern: '^$|^[a-z_][a-z0-9_-]{0,31}$'

  AdminSshPublicKey:
    Type: String
    Default: ''
    Description: >
      Optional OpenSSH public key for AdminUsername (e.g. 'ssh-ed25519 AAAA... me@lab').
      A PUBLIC key is not a secret, so unlike every other credential here it is safe
      as a CFN parameter. Only useful if Tailscale SSH is not the path you want; the
      security group has zero inbound rules either way, so this is still tailnet-only.

  NetdataStreamKeyParam:
    Type: String
    Default: /monitoring/netdata-stream-key
    Description: SSM SecureString holding the Netdata streaming API key (a UUID). Home children use the same value.

  GrafanaPasswordParam:
    Type: String
    Default: /monitoring/grafana-admin-password
    Description: SSM SecureString the instance WRITES a generated Grafana admin password to on first boot.

  BootstrapLogGroupName:
    Type: String
    Default: /tavlab/monitoring-bootstrap
    Description: >
      CloudWatch Logs group the instance ships its bootstrap log to, on both
      success and failure. Created by the instance rather than by this stack on
      purpose: a CFN-managed group would either be deleted by the rollback that
      you need it to survive, or collide on the next deploy attempt.

  AlertEmail:
    Type: String
    Default: ''
    Description: >
      Optional. Creates an SNS topic and CloudWatch alarms on EC2 status checks, so you
      get told if the monitoring node itself dies. Leave blank to skip.

  LatestUbuntuAmi:
    Type: AWS::SSM::Parameter::Value<AWS::EC2::Image::Id>
    Default: /aws/service/canonical/ubuntu/server/24.04/stable/current/arm64/hvm/ebs-gp3/ami-id
    Description: Resolved at deploy time from Canonical's public SSM parameter. Do not change unless you know why.

Conditions:
  HasAlertEmail: !Not [!Equals [!Ref AlertEmail, '']]
  UsePublicIpv4: !Equals [!Ref AssignPublicIpv4, 'true']
  HasAdminUser: !Not [!Equals [!Ref AdminUsername, '']]

Resources:

  # ---------------------------------------------------------------- IAM
  # No SSH key pair is created anywhere in this stack. Access is via Tailscale
  # SSH, or SSM Session Manager as break-glass if Tailscale fails to come up.
  InstanceRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal: {Service: ec2.amazonaws.com}
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - !Sub 'arn:${AWS::Partition}:iam::aws:policy/AmazonSSMManagedInstanceCore'
      Policies:
        - PolicyName: bootstrap
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Sid: ReadBootstrapSecrets
                Effect: Allow
                Action: ssm:GetParameter
                Resource:
                  - !Sub 'arn:${AWS::Partition}:ssm:${AWS::Region}:${AWS::AccountId}:parameter${TailscaleAuthKeyParam}'
                  - !Sub 'arn:${AWS::Partition}:ssm:${AWS::Region}:${AWS::AccountId}:parameter${NetdataStreamKeyParam}'
              - Sid: WriteGeneratedGrafanaPassword
                Effect: Allow
                Action: ssm:PutParameter
                Resource: !Sub 'arn:${AWS::Partition}:ssm:${AWS::Region}:${AWS::AccountId}:parameter${GrafanaPasswordParam}'
              - Sid: SsmKmsViaParameterStore
                Effect: Allow
                Action: [kms:Decrypt, kms:Encrypt]
                Resource: '*'
                Condition:
                  StringEquals:
                    kms:ViaService: !Sub 'ssm.${AWS::Region}.amazonaws.com'
              - Sid: ShipBootstrapLogToCloudWatch
                Effect: Allow
                Action:
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                  - logs:PutRetentionPolicy
                Resource:
                  - !Sub 'arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:${BootstrapLogGroupName}'
                  - !Sub 'arn:${AWS::Partition}:logs:${AWS::Region}:${AWS::AccountId}:log-group:${BootstrapLogGroupName}:*'
              - Sid: SignalBootstrapResult
                Effect: Allow
                Action: cloudformation:SignalResource
                Resource: !Sub 'arn:${AWS::Partition}:cloudformation:${AWS::Region}:${AWS::AccountId}:stack/${AWS::StackName}/*'

  InstanceProfile:
    Type: AWS::IAM::InstanceProfile
    Properties:
      Roles: [!Ref InstanceRole]

  # ---------------------------------------------------------------- Security
  # Deliberately no SecurityGroupIngress. Tailscale is outbound-only (UDP 41641
  # plus HTTPS to the coordination server) and Netdata children / Alloy PUSH to
  # this node over the tailnet, so nothing ever needs to reach it from the
  # internet. Egress is open so it can reach package repos, SSM and DERP relays.
  InstanceSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Monitoring node - no inbound, all outbound
      VpcId: !ImportValue
        Fn::Sub: '${NetworkStackName}-VpcId'
      SecurityGroupEgress:
        - Description: All IPv4 egress
          IpProtocol: '-1'
          CidrIp: 0.0.0.0/0
        - Description: All IPv6 egress
          IpProtocol: '-1'
          CidrIpv6: ::/0
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-sg'

  # ---------------------------------------------------------------- Instance
  MonitorInstance:
    Type: AWS::EC2::Instance
    CreationPolicy:
      ResourceSignal:
        Count: 1
        Timeout: PT25M
    Properties:
      ImageId: !Ref LatestUbuntuAmi
      InstanceType: !Ref InstanceType
      IamInstanceProfile: !Ref InstanceProfile
      Monitoring: false
      NetworkInterfaces:
        - DeviceIndex: '0'
          SubnetId: !ImportValue
            Fn::Sub: '${NetworkStackName}-SubnetId'
          GroupSet: [!Ref InstanceSecurityGroup]
          AssociatePublicIpAddress: !If [UsePublicIpv4, true, false]
          Ipv6AddressCount: 1
          DeleteOnTermination: true
      MetadataOptions:
        HttpTokens: required
        HttpEndpoint: enabled
      BlockDeviceMappings:
        - DeviceName: /dev/sda1
          Ebs:
            VolumeType: gp3
            VolumeSize: 20
            Encrypted: true
            DeleteOnTermination: true
        # Data volume is a block device mapping rather than a separate
        # AWS::EC2::Volume + VolumeAttachment on purpose: a VolumeAttachment
        # DependsOn the instance, but the instance is not CREATE_COMPLETE until
        # it signals, and it cannot signal until /data is mounted. That is a
        # deadlock. This avoids it.
        - DeviceName: /dev/sdf
          Ebs:
            VolumeType: gp3
            VolumeSize: !Ref DataVolumeSizeGb
            Encrypted: true
            DeleteOnTermination: !Ref DataVolumeDeleteOnTermination
      Tags:
        - Key: Name
          Value: !Sub '${AWS::StackName}-${TailscaleHostname}'
        - Key: Role
          Value: monitoring
        - Key: DeploymentMode
          Value: !Ref DeploymentMode
      UserData:
        Fn::Base64: !Sub |
          #!/bin/bash
          # -e matters: on any failure the EXIT trap signals FAILURE to CFN, so a
          # broken bootstrap rolls back immediately instead of waiting out the
          # 25-minute timeout.
          set -euxo pipefail
          exec > >(tee -a /var/log/monitoring-bootstrap.log) 2>&1

          export DEBIAN_FRONTEND=noninteractive
          export AWS_DEFAULT_REGION=${AWS::Region}
          export HOME=/root
          # Wait out unattended-upgrades rather than racing it for the dpkg lock.
          APT_OPTS="-o DPkg::Lock::Timeout=600"

          RESULT=FAILURE
          STAGE=init
          FAILED_CMD=""
          FAILED_LINE=""
          LG="${BootstrapLogGroupName}"
          BOOTLOG=/var/log/monitoring-bootstrap.log
          SHIPPED=0
          LOGREADY=0

          # IMDSv2 needs no AWS CLI, so get the instance id immediately: the EXIT
          # trap needs it to signal CFN even if we die in the first few seconds.
          IMDS_TOKEN=$(curl -sX PUT "http://169.254.169.254/latest/api/token" \
            -H "X-aws-ec2-metadata-token-ttl-seconds: 21600" || true)
          IID=$(curl -s -H "X-aws-ec2-metadata-token: $IMDS_TOKEN" \
            http://169.254.169.254/latest/meta-data/instance-id || true)

          # Record where we died. Expanded when the trap fires, not now.
          trap 'FAILED_CMD=$BASH_COMMAND; FAILED_LINE=$LINENO' ERR

          # Ship the bootstrap log to CloudWatch Logs so it SURVIVES ROLLBACK --
          # otherwise a failed stack deletes the instance and the only explanation
          # of the failure with it. Ships only the bytes written since the last
          # call, so 'aws logs tail --follow' shows progress LIVE rather than
          # nothing at all until the bootstrap is already over.
          ship_chunk() {
            { set +x; } 2>/dev/null
            if [ "$LOGREADY" = "1" ]; then
              local total
              total=$(wc -c < "$BOOTLOG" 2>/dev/null | tr -d ' ' || echo 0)
              if [ "$total" -gt "$SHIPPED" ]; then
                # Defence in depth. Secret-handling runs with xtrace off, but a
                # future edit will forget, and this log gets shipped to CloudWatch
                # and pasted into diag files that end up in git.
                tail -c "+$((SHIPPED + 1))" "$BOOTLOG" 2>/dev/null \
                  | tail -c 200000 \
                  | sed -E -e 's/tskey-[A-Za-z0-9_-]+/tskey-REDACTED/g' \
                           -e 's/(authkey|api[-_ ]?key|password|PASSWORD)([ =:"]+)[^ "]+/\1\2REDACTED/g' \
                  > /tmp/chunk.txt || true
                SHIPPED="$total"
                if [ -s /tmp/chunk.txt ]; then
                  python3 -c "import json,time;print(json.dumps([{'timestamp':int(time.time()*1000),'message':open('/tmp/chunk.txt',errors='replace').read()}]))" \
                    > /tmp/logevents.json 2>/dev/null || true
                  aws logs put-log-events --log-group-name "$LG" \
                    --log-stream-name "$IID" \
                    --log-events file:///tmp/logevents.json >/dev/null 2>&1 || true
                fi
              fi
            fi
            set -x
          }

          # Every stage boundary flushes to CloudWatch, so the last stage you can
          # see in 'aws logs tail' is where the bootstrap currently is (or died).
          stage() { STAGE="$1"; echo "=== STAGE: $1"; ship_chunk; }

          finish() {
            RC=$?
            trap - ERR
            # tee is a separate process; give it a moment to flush the trace
            # output of whatever just failed before we read the file.
            sleep 2
            echo "=== result=$RESULT stage=$STAGE exit=$RC"
            if [ -n "$FAILED_CMD" ]; then
              echo "=== failed near line $FAILED_LINE running: $FAILED_CMD"
            fi
            sleep 1
            ship_chunk
            aws cloudformation signal-resource \
              --stack-name ${AWS::StackName} \
              --logical-resource-id MonitorInstance \
              --unique-id "$IID" \
              --status "$RESULT" || true
          }
          trap finish EXIT

          stage apt
          apt-get $APT_OPTS update -y
          apt-get $APT_OPTS install -y curl unzip ca-certificates gnupg jq rsync \
            python3 util-linux

          # ---- AWS CLI v2 (Ubuntu's packaged awscli lags; we need signal-resource)
          stage awscli
          curl -fsSL https://awscli.amazonaws.com/awscli-exe-linux-aarch64.zip -o /tmp/awscliv2.zip
          unzip -q -o /tmp/awscliv2.zip -d /tmp
          /tmp/aws/install --update
          hash -r
          aws --version

          # Open the CloudWatch channel before anything that can fail
          # interestingly. From here on every stage() streams to it.
          aws logs create-log-group --log-group-name "$LG" >/dev/null 2>&1 || true
          aws logs put-retention-policy --log-group-name "$LG" \
            --retention-in-days 14 >/dev/null 2>&1 || true
          aws logs create-log-stream --log-group-name "$LG" \
            --log-stream-name "$IID" >/dev/null 2>&1 || true
          LOGREADY=1
          stage logsetup

          # ---- fetch secrets from SSM (never in UserData / stack history)
          # Retried: IMDS role credentials can lag the instance by a few seconds,
          # and a transient throttle here should not kill the whole deploy.
          stage ssm
          get_param() {
            n=0
            until [ "$n" -ge 5 ]; do
              if v=$(aws ssm get-parameter --name "$1" --with-decryption \
                       --query Parameter.Value --output text 2>/tmp/ssm.err); then
                printf '%s' "$v"
                return 0
              fi
              n=$((n+1))
              echo "ssm get-parameter $1 attempt $n failed: $(cat /tmp/ssm.err)"
              sleep 5
            done
            return 1
          }
          # xtrace is on for diagnostics, and it expands variables -- so anything
          # touching a secret must run with it OFF, or the plaintext lands in the
          # bootstrap log, then CloudWatch, then a diag file, then possibly a git
          # commit. Learned the hard way: an auth key leaked exactly that route.
          { set +x; } 2>/dev/null
          TSKEY=$(get_param "${TailscaleAuthKeyParam}")
          NDKEY=$(get_param "${NetdataStreamKeyParam}")
          set -x
          test -n "$TSKEY"
          test -n "$NDKEY"
          echo "fetched both secrets from SSM ok (values suppressed)"

          # ---- find, format and mount the data volume
          # /dev/sdf shows up as an nvme namespace on Nitro, so identify it as
          # "the disk that is not the one holding /".
          stage volume
          echo "--- block devices as seen at this point:"
          lsblk -o NAME,TYPE,SIZE,FSTYPE,MOUNTPOINTS || true
          findmnt -no SOURCE / || true

          ROOT_SRC=$(findmnt -no SOURCE / || true)
          ROOT_DISK=$(lsblk -no PKNAME "$ROOT_SRC" 2>/dev/null | head -1 | tr -d ' ' || true)
          if [ -z "$ROOT_DISK" ]; then
            # Fallback: walk the tree and take the disk whose partition holds /.
            ROOT_DISK=$(lsblk -rno NAME,TYPE,MOUNTPOINTS \
              | awk '$2=="disk"{d=$1} $3=="/"{print d; exit}' || true)
          fi
          # Refuse to guess if the root disk cannot be positively identified --
          # otherwise the loop below could select it and mount it at /data.
          echo "root source=$ROOT_SRC root disk=$ROOT_DISK"
          test -n "$ROOT_DISK"

          DEV=""
          for i in $(seq 1 60); do
            CAND=$(lsblk -dn -o NAME,TYPE | awk '$2=="disk"{print $1}' \
              | grep -v "^$ROOT_DISK$" | head -1 || true)
            if [ -n "$CAND" ] && [ -b "/dev/$CAND" ]; then DEV="/dev/$CAND"; break; fi
            echo "waiting for data volume ($i)"; sleep 5
          done
          echo "data device=$DEV"
          test -n "$DEV"

          if ! blkid "$DEV" >/dev/null 2>&1; then
            mkfs.ext4 -L mondata "$DEV"
          fi
          mkdir -p /data

          # udev needs to settle before blkid can see a just-created filesystem.
          # Without this retry the UUID can come back empty and fstab gets a
          # bare "UUID=" line, which then fails at mount -a.
          udevadm settle || true
          FSUUID=""
          for i in $(seq 1 12); do
            FSUUID=$(blkid -s UUID -o value "$DEV" 2>/dev/null || true)
            [ -n "$FSUUID" ] && break
            echo "waiting for filesystem UUID on $DEV ($i)"
            udevadm settle || true
            sleep 5
          done
          echo "data fs uuid=$FSUUID"
          test -n "$FSUUID"

          grep -q "$FSUUID" /etc/fstab || \
            echo "UUID=$FSUUID /data ext4 defaults,nofail 0 2" >> /etc/fstab
          mount -a
          mountpoint -q /data

          mkdir -p /data/netdata/cache /data/kuma /data/ntfy/cache /data/ntfy/lib /data/loki /data/grafana
          chown -R 10001:10001 /data/loki
          chown -R 472:472 /data/grafana

          # ---- Docker CE from upstream
          stage docker
          CODENAME=$(. /etc/os-release && echo $VERSION_CODENAME)
          install -m 0755 -d /etc/apt/keyrings
          curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
          chmod a+r /etc/apt/keyrings/docker.asc
          echo "deb [arch=arm64 signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu $CODENAME stable" \
            > /etc/apt/sources.list.d/docker.list

          # ---- Tailscale from upstream
          stage tailscale_pkg
          curl -fsSL "https://pkgs.tailscale.com/stable/ubuntu/$CODENAME.noarmor.gpg" \
            -o /usr/share/keyrings/tailscale-archive-keyring.gpg
          curl -fsSL "https://pkgs.tailscale.com/stable/ubuntu/$CODENAME.tailscale-keyring.list" \
            -o /etc/apt/sources.list.d/tailscale.list

          apt-get $APT_OPTS update -y
          apt-get $APT_OPTS install -y docker-ce docker-ce-cli containerd.io \
            docker-buildx-plugin docker-compose-plugin tailscale
          systemctl enable --now docker

          # Cap container log growth so /var never fills silently.
          cat > /etc/docker/daemon.json <<'DOCKERJSON'
          {
            "log-driver": "json-file",
            "log-opts": {"max-size": "20m", "max-file": "5"}
          }
          DOCKERJSON
          systemctl restart docker

          # ---- join the tailnet
          stage tailscale_up
          TS_ARGS=""
          if [ -n "${TailscaleTag}" ]; then TS_ARGS="--advertise-tags=${TailscaleTag}"; fi
          # Trace off: the expanded command line contains the auth key. The error
          # message from tailscale itself is still captured, which is the part that
          # actually matters -- a bad key reports 'invalid key: API key ... not valid'.
          { set +x; } 2>/dev/null
          echo "+ tailscale up --authkey <redacted> --hostname ${TailscaleHostname} --ssh $TS_ARGS"
          tailscale up --authkey "$TSKEY" --hostname "${TailscaleHostname}" --ssh $TS_ARGS
          set -x
          BIND_IP=""
          for i in $(seq 1 30); do
            BIND_IP=$(tailscale ip -4 2>/dev/null | head -1 || true)
            [ -n "$BIND_IP" ] && break
            echo "waiting for tailscale ip ($i)"; sleep 5
          done
          test -n "$BIND_IP"
          echo "tailnet address: $BIND_IP"

          # ---- optional named local admin
          #
          # Deliberately PASSWORDLESS. 'tailscale up --ssh' above means SSH to this
          # node is authenticated by tailnet identity: the ACL decides who may
          # connect and as which local user, and no password or key is involved.
          # Adding a password here would not add security, it would subtract it --
          # it creates a guessable credential where there was none, and it would be
          # the only credential on the box not held in SSM.
          #
          # Consequence worth understanding: because the password is LOCKED, group
          # membership in 'sudo' alone leaves sudo UNUSABLE -- it would prompt for a
          # password that can never be satisfied, and you would be locked out of root
          # on a box whose whole point is that you can get in. Hence the explicit
          # NOPASSWD sudoers drop-in. This is the same shape as the AMI's own
          # /etc/sudoers.d/90-cloud-init-users for the 'ubuntu' user, so it is not a
          # weakening relative to what AWS already ships.
          stage adminuser
          if [ -n "${AdminUsername}" ]; then
            if id -u "${AdminUsername}" >/dev/null 2>&1; then
              echo "user ${AdminUsername} already exists, adjusting"
            else
              useradd -m -s /bin/bash "${AdminUsername}"
            fi
            # docker group is root-equivalent, but so is the NOPASSWD sudo below --
            # it grants no privilege the account does not already have, it only saves
            # typing sudo in front of every docker command.
            usermod -aG sudo,docker,adm,systemd-journal "${AdminUsername}"
            passwd -l "${AdminUsername}"
            # visudo -cf before installing: a malformed sudoers file breaks sudo for
            # EVERYONE, including the recovery path. Write to a temp name, validate,
            # then move it into place.
            printf '%s ALL=(ALL) NOPASSWD:ALL\n' "${AdminUsername}" > /tmp/sudoer.new
            visudo -cf /tmp/sudoer.new
            install -m 0440 -o root -g root /tmp/sudoer.new "/etc/sudoers.d/90-${AdminUsername}"
            rm -f /tmp/sudoer.new
            # A public key is not a secret, so no xtrace guard. This is a fallback
            # path only: with zero inbound SG rules it is still reachable exclusively
            # over the tailnet, so it buys redundancy against Tailscale SSH being
            # disabled in the ACL, not against Tailscale being down.
            if [ -n "${AdminSshPublicKey}" ]; then
              HOMEDIR=$(getent passwd "${AdminUsername}" | cut -d: -f6)
              install -d -m 0700 -o "${AdminUsername}" -g "${AdminUsername}" "$HOMEDIR/.ssh"
              echo "${AdminSshPublicKey}" > "$HOMEDIR/.ssh/authorized_keys"
              chmod 0600 "$HOMEDIR/.ssh/authorized_keys"
              chown "${AdminUsername}:${AdminUsername}" "$HOMEDIR/.ssh/authorized_keys"
            fi
            echo "admin user ${AdminUsername} ready (password locked, NOPASSWD sudo)"
          else
            echo "no AdminUsername set; use the AMI's 'ubuntu' account"
          fi

          # ---- Netdata (host install, acts as the streaming parent)
          stage netdata
          curl -fsSL https://get.netdata.cloud/kickstart.sh -o /tmp/netdata-kickstart.sh
          sh /tmp/netdata-kickstart.sh --non-interactive --stable-channel --disable-telemetry

          systemctl stop netdata || true
          # Bind-mount the dbengine cache onto /data. Version-proof: avoids guessing
          # which netdata.conf section names the cache directory in this release.
          rsync -a /var/cache/netdata/ /data/netdata/cache/ || true
          grep -q '/var/cache/netdata' /etc/fstab || \
            echo "/data/netdata/cache /var/cache/netdata none bind,nofail 0 0" >> /etc/fstab
          mount -a
          chown -R netdata:netdata /var/cache/netdata || true

          # Accept streams from home children. Their source addresses are tailnet
          # CGNAT addresses. Netdata stays bound to all interfaces because it can
          # start before tailscale0 exists on boot; the empty security group and
          # this allow-list are the actual controls.
          # Trace off: the section header is the streaming key.
          # 'allow from' takes netdata SIMPLE PATTERNS -- shell-style globs -- and
          # NOT CIDR. This previously read '100.64.0.0/10 127.0.0.1', which netdata
          # compares as a literal string against the peer address. It therefore
          # matched nothing, and every child was refused while the parent itself
          # still worked because 127.0.0.1 happened to be a literal match.
          #
          # That failure is close to undiagnosable from the outside: the key was
          # correct, the parent was healthy and reachable, no rejection appeared in
          # the parent's node list, and the parent kept mirroring itself so it looked
          # alive. Cost roughly a day. Confirmed 2026-09-25 by widening to '*', at
          # which point pve appeared within 20 seconds.
          #
          # '100.*' is wider than Tailscale's CGNAT range (100.64.0.0/10), because a
          # glob cannot express 100.64-100.127 without five error-prone alternations.
          # That is acceptable here and ONLY here: the security group has zero inbound
          # rules, so :19999 is unreachable except across the tailnet. This ACL is
          # defence in depth, not the boundary. Do not copy it somewhere exposed.
          { set +x; } 2>/dev/null
          cat > /etc/netdata/stream.conf <<STREAMCONF
          [$NDKEY]
              enabled = yes
              default memory mode = dbengine
              health enabled by default = auto
              allow from = 100.* 127.0.0.1
          STREAMCONF
          set -x
          echo "wrote /etc/netdata/stream.conf (api key suppressed)"
          chown netdata:netdata /etc/netdata/stream.conf
          chmod 640 /etc/netdata/stream.conf

          # health_alarm_notify.conf is sourced as shell, so appended assignments win.
          cat >> /etc/netdata/health_alarm_notify.conf <<NOTIFYCONF

          # --- managed by CloudFormation bootstrap ---
          SEND_NTFY="YES"
          DEFAULT_RECIPIENT_NTFY="http://127.0.0.1:8080/netdata"
          NOTIFYCONF
          systemctl start netdata

          # ---- generate a Grafana admin password and stash it in SSM
          stage grafana_pw
          # hex, not base64-piped-through-tr: a pipeline here can SIGPIPE and
          # abort the script under 'set -o pipefail'.
          # Trace off: this is a credential from the moment it exists.
          { set +x; } 2>/dev/null
          GFPASS=$(openssl rand -hex 16)
          aws ssm put-parameter --name "${GrafanaPasswordParam}" --type SecureString \
            --value "$GFPASS" --overwrite >/dev/null
          printf '%s\n' "$GFPASS" > /root/.grafana_admin_password
          set -x
          echo "generated Grafana admin password and stored it in SSM (value suppressed)"
          chmod 600 /root/.grafana_admin_password

          # ---- the container stack
          stage compose
          mkdir -p /opt/monitoring/provisioning/datasources

          cat > /opt/monitoring/docker-compose.yml <<COMPOSE
          services:
            uptime-kuma:
              image: louislam/uptime-kuma:1
              container_name: uptime-kuma
              restart: unless-stopped
              volumes:
                - /data/kuma:/app/data
              ports:
                - "$BIND_IP:3001:3001"

            ntfy:
              image: binwiederhier/ntfy:v2
              container_name: ntfy
              command: serve
              restart: unless-stopped
              environment:
                NTFY_BASE_URL: "http://$BIND_IP:8080"
                NTFY_CACHE_FILE: /var/cache/ntfy/cache.db
                NTFY_AUTH_FILE: /var/lib/ntfy/auth.db
                NTFY_AUTH_DEFAULT_ACCESS: read-write
                NTFY_ATTACHMENT_CACHE_DIR: /var/cache/ntfy/attachments
              volumes:
                - /data/ntfy/cache:/var/cache/ntfy
                - /data/ntfy/lib:/var/lib/ntfy
              ports:
                - "127.0.0.1:8080:80"
                - "$BIND_IP:8080:80"

            loki:
              image: grafana/loki:3.1.1
              container_name: loki
              profiles: ["full"]
              restart: unless-stopped
              user: "10001:10001"
              command: -config.file=/etc/loki/config.yml
              volumes:
                - /opt/monitoring/loki.yml:/etc/loki/config.yml:ro
                - /data/loki:/loki
              ports:
                - "$BIND_IP:3100:3100"

            grafana:
              image: grafana/grafana-oss:11.2.0
              container_name: grafana
              profiles: ["full"]
              restart: unless-stopped
              user: "472:472"
              depends_on: [loki]
              environment:
                GF_SECURITY_ADMIN_PASSWORD: "$GFPASS"
                GF_USERS_ALLOW_SIGN_UP: "false"
                GF_SERVER_ROOT_URL: "http://$BIND_IP:3000"
              volumes:
                - /data/grafana:/var/lib/grafana
                - /opt/monitoring/provisioning:/etc/grafana/provisioning
              ports:
                - "$BIND_IP:3000:3000"
          COMPOSE

          cat > /opt/monitoring/loki.yml <<'LOKICONF'
          auth_enabled: false
          server:
            http_listen_port: 3100
            log_level: warn
          common:
            instance_addr: 127.0.0.1
            path_prefix: /loki
            storage:
              filesystem:
                chunks_directory: /loki/chunks
                rules_directory: /loki/rules
            replication_factor: 1
            ring:
              kvstore:
                store: inmemory
          schema_config:
            configs:
              - from: 2024-01-01
                store: tsdb
                object_store: filesystem
                schema: v13
                index:
                  prefix: index_
                  period: 24h
          limits_config:
            retention_period: 720h
            reject_old_samples: false
            allow_structured_metadata: true
          compactor:
            working_directory: /loki/compactor
            delete_request_store: filesystem
            retention_enabled: true
          LOKICONF

          # Only Loki is provisioned. Netdata's own UI on :19999 is the metrics
          # front end -- its /api/v1/allmetrics endpoint is a Prometheus *scrape*
          # target, not a query API, so it cannot back a Grafana Prometheus
          # datasource. Add real Prometheus later if you want that.
          cat > /opt/monitoring/provisioning/datasources/loki.yml <<'DSCONF'
          apiVersion: 1
          datasources:
            - name: Loki
              type: loki
              access: proxy
              url: http://loki:3100
              isDefault: true
              jsonData:
                maxLines: 5000
          DSCONF

          stage compose_up
          cd /opt/monitoring
          if [ "${DeploymentMode}" = "FullStack" ]; then
            docker compose --profile full up -d
          else
            docker compose up -d
          fi

          RESULT=SUCCESS
          echo "bootstrap complete"

  # ---------------------------------------------------------------- Self-watch
  # The monitoring node cannot alert on its own death. These alarms live in the
  # AWS control plane, outside the instance, and are effectively free.
  AlertTopic:
    Type: AWS::SNS::Topic
    Condition: HasAlertEmail
    Properties:
      DisplayName: !Sub '${AWS::StackName}-alerts'

  # The subscription MUST be its own resource, not an inline 'Subscription:'
  # property on the topic above. CloudFormation treats the inline form as
  # write-once: it creates the subscription with the topic and then never
  # reconciles it again. So if the subscription disappears -- and it disappears
  # very easily, because every SNS notification email carries an unsubscribe
  # link in its footer -- CFN will not notice and will not recreate it. The
  # stack stays CREATE_COMPLETE while alerting is silently dead, which is the
  # exact failure this self-watch exists to prevent.
  #
  # As a standalone resource it is a real managed resource: drift is detectable,
  # and a stack update recreates it if it was removed out of band.
  #
  # This bit us on 2026-09-25: the topic existed, both alarms were OK, and the
  # topic had zero subscriptions. An earlier auto-recovery email had been
  # delivered, so the subscription had existed and been confirmed at some point.
  AlertSubscription:
    Type: AWS::SNS::Subscription
    Condition: HasAlertEmail
    Properties:
      TopicArn: !Ref AlertTopic
      Protocol: email
      Endpoint: !Ref AlertEmail

  # TreatMissingData MUST be 'missing' on both of these, not 'breaching'.
  #
  # StatusCheckFailed_* is only published while the instance is RUNNING. It is
  # absent during pending, stopping, stopped, starting -- and during an
  # auto-recovery, which is itself a stop/start. Treating absence as a failure
  # therefore creates a loop on the system alarm: recover stops the instance ->
  # metric goes missing -> missing counts as breaching -> alarm re-fires recover.
  # The instance gets stopped/started repeatedly and looks "unresponsive".
  #
  # It is also wrong on a brand-new instance, which publishes no status-check
  # datapoints for the first couple of minutes. A fresh alarm with 'breaching'
  # goes straight to ALARM on an instance that is perfectly healthy.
  SystemStatusAlarm:
    Type: AWS::CloudWatch::Alarm
    Condition: HasAlertEmail
    Properties:
      AlarmName: !Sub '${AWS::StackName}-system-status-failed'
      AlarmDescription: >
        Underlying AWS host is unhealthy. Recovers the instance automatically.
        NOTE: recovery preserves the instance id, so cloud-init will NOT re-run
        UserData afterwards. That is fine once the node is built, but if recovery
        fires during the initial bootstrap the stack can never be signalled and
        will time out -- redeploy rather than waiting.
      Namespace: AWS/EC2
      MetricName: StatusCheckFailed_System
      Dimensions:
        - Name: InstanceId
          Value: !Ref MonitorInstance
      Statistic: Maximum
      Period: 60
      # 2 of the last 3 minutes: rides out a single scraped datapoint without
      # waiting long enough to matter for a genuine host failure.
      EvaluationPeriods: 3
      DatapointsToAlarm: 2
      Threshold: 0
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: missing
      AlarmActions:
        - !Ref AlertTopic
        - !Sub 'arn:${AWS::Partition}:automate:${AWS::Region}:ec2:recover'

  InstanceStatusAlarm:
    Type: AWS::CloudWatch::Alarm
    Condition: HasAlertEmail
    Properties:
      AlarmName: !Sub '${AWS::StackName}-instance-status-failed'
      AlarmDescription: Instance-level status check failing (OS wedged, network gone).
      Namespace: AWS/EC2
      MetricName: StatusCheckFailed_Instance
      Dimensions:
        - Name: InstanceId
          Value: !Ref MonitorInstance
      Statistic: Maximum
      Period: 60
      EvaluationPeriods: 5
      DatapointsToAlarm: 3
      Threshold: 0
      ComparisonOperator: GreaterThanThreshold
      TreatMissingData: missing
      AlarmActions: [!Ref AlertTopic]

Outputs:
  InstanceId:
    Value: !Ref MonitorInstance

  DeploymentMode:
    Value: !Ref DeploymentMode

  NetdataUrl:
    Description: Metrics UI, over the tailnet only.
    Value: !Sub 'http://${TailscaleHostname}:19999'

  UptimeKumaUrl:
    Description: Synthetic checks and status page. First visit sets the admin user.
    Value: !Sub 'http://${TailscaleHostname}:3001'

  NtfyUrl:
    Description: Alert fan-out. Subscribe your phone to the 'netdata' topic.
    Value: !Sub 'http://${TailscaleHostname}:8080'

  GrafanaUrl:
    Description: FullStack only. Log exploration over Loki.
    Value: !Sub 'http://${TailscaleHostname}:3000'

  GrafanaPasswordCommand:
    Description: Retrieve the generated Grafana admin password.
    Value: !Sub 'aws ssm get-parameter --name ${GrafanaPasswordParam} --with-decryption --query Parameter.Value --output text'

  AdminSshCommand:
    Description: >
      Interactive shell over Tailscale SSH -- no key, no password, tailnet identity
      only. Falls back to 'ubuntu' when AdminUsername was left blank; that account
      ships with the AMI and already has passwordless sudo.
    Value: !Sub
      - 'ssh ${User}@${TailscaleHostname}'
      - User: !If [HasAdminUser, !Ref AdminUsername, 'ubuntu']

  BreakGlassAccess:
    Description: >
      Use if Tailscale fails to come up -- this is the ONLY path that does not
      depend on the tailnet, since the security group has no inbound rules at all.
      Requires the session-manager-plugin installed locally.
    Value: !Sub 'aws ssm start-session --target ${MonitorInstance}'

  BootstrapLog:
    Description: Where to look on the instance itself.
    Value: /var/log/monitoring-bootstrap.log

  BootstrapLogCommand:
    Description: >
      Read the bootstrap log from CloudWatch. This SURVIVES rollback, so use it to
      diagnose a CREATE_FAILED even after the instance has been deleted.
    Value: !Sub 'aws logs tail ${BootstrapLogGroupName} --since 2h'

  ChildStreamConfigHint:
    Description: What to put in /etc/netdata/stream.conf on each home node.
    Value: !Sub '[stream] enabled=yes destination=${TailscaleHostname}:19999 api key=<value of ${NetdataStreamKeyParam}>'
